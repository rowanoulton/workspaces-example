{
  "name": "@slack/foo",
  "version": "0.0.0",
  "scripts": {
    "lint": "eslint **/*.js",
    "test": "echo testing foo",
    "build": "esbuild --bundle foo.ts --outdir=dist"
  },
  "dependencies": {
    "@slack/built-thing": "workspace:*",
    "@slack/raw-thing": "workspace:*",
    "@slack/typed-thing": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.17.4",
    "eslint": "latest"
  }
}
